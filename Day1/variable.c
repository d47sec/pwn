#include <stdio.h>
int main(int argc, char const *argv[])
{
    int x;
    const double PI = 3.14;
    printf("Size of int %d\n", sizeof x);
    printf("%0.2f\n", PI); // format de lay so phan thap phan o trong c 
    // %f => float
    // %lf => double 
    return 0;
}
